import { blocks_content as core } from "./core";
import { blocks_content as genesis } from "./genesis";
import { blocks_content as kadence } from "./kadence";
import { blocks_content as melonpan } from "./melonpan";
import { blocks_content as stackable } from "./stackable";

export const data = {
	core,
	genesis,
	kadence,
	melonpan,
	stackable,
};
