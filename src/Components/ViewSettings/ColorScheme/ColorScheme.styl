/**
 * ColorScheme
 */

.container
	background-color var(--primary_color)
	padding 2em 1.2em

	select

		&
		&:hover
		&:focus
			color var(--color_1)
			border-color var(--color_1)
			background-color var(--primary_color)
			outline-color transparent
			box-shadow none
