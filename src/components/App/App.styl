/**
 * App
 */

.container
	overflow hidden
	background-color var(--background_color)
	color var(--color)
	fill var(--svg_color)
	height 100%
	width 100%

	*
		user-select none

.calculating_size
	display flex
	flex 1
	flex-direction column
