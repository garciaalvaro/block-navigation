const { __ } = wp.i18n;

const plugin_title = __("Block Navigation");
const plugin_namespace = "block-navigation";
const pr = "bn"; // prefix
const pr_store = `melonpan/${plugin_namespace}`; // prefix

export { plugin_title, pr, pr_store, plugin_namespace };
