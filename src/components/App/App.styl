/**
 * App
 */

.container
	overflow hidden
	background-color var(--background_color)
	color var(--color)
	fill var(--svg_color)
	height 100%
	width 100%

	*
		user-select none

.is_detached
	outline 1px solid var(--detached_bordercolor)

.is_expanded
	height 300px

:global(#block-navigation-detached)
	position fixed
	bottom 0
	left 0
	width 240px
	z-index 99999

	*
		box-sizing border-box
